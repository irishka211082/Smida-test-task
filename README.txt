ТЕСТОВЕ ЗАВДАННЯ:

Термін виконання - тиждень.
Суть завдання - реєстр акцій
Критерії якості - чистий, зрозумілий і структурований код. Мінімальний набір технологій: Java7 + Spring Framework + БД (PostgreSQL або H2)
Збірка - Maven. Зовнішнє оформлення на розсуд кандидата.
Звіт про завдання:
Посилання проекту на репозиторій (Github, Bitbucket).
Посилання на модель спроектованої БД
(Опціонально) Виконане завдання можна супроводити текстовим описом

Мета тестового завдання - оцінити рівень знань кандидата за технологіями:
1. Java Core (Collections Framework, Servlet API)
2. Rest service (json)
3. БД - JDBC, JPA,

Додаток має складатися з таких частин:
1. База даних на платформі PostgreSQL або H2
2. Система управління міграціями бази даних liquibase
3. Rest-сервіс, який приймає запити по протоколу http

Вимоги до компонентів.
1) Реалізувати сервіс створення запису.
a) Сутність містить поля: Коментар,
ЄДРПОУ, кількість, загальна номінальна вартість, номінальна вартість, дата випуску, статус.
b) загальна номінальна вартість формується номінальна вартість * кількість.
c) Статус при створенні 'active'

2) Реалізувати сервіс редагування записe (всі зміни повинні записуватися в історію)
a) При зміні в історію має записатися ЄДРПОУ, назва поля яку змінено, старе значення поля, нове значення.

3) Реалізувати сервіс видалення запису (не фізично, при видаленні змінюється статус 'deleted')

4) Реалізувати сервіс отримання записів (можливість фільтрації по полях: статус, ЄДРПОУ, якщо фільтр не переданий віддавати весь список, пейджінація за замовчуванням 10 записів, якщо параметр не переданий явно)

5) Наповнення даними основної сутності при старті проекту з файлу json. Всі дані заповнити в довільній формі.

Уточнюючі питання задавати в цьому гугл-документі через коментарі.

Виконане завдання надсилати на 2 емайли:
d.grek@smida.gov.ua
v.zaytsev@smida.gov.ua
